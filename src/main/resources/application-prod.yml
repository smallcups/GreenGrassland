# 生产环境配置
spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.greengrassland: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN

# GreenGrassland 产品需求文档 v2.0

> 基于 2026-05-12 产品经理审查，聚焦用户体验提升和产品合规。
> 技术功能清单详见 [TODO.md](./TODO.md)

## 一、P0 — 阻塞上线，必须修复

### 1.1 登录/注册分离

**现状**：登录和注册合并在一个表单，用户输入用户名密码后系统自动判断"有账号就登录，没账号就注册"。

**问题**：
- 用户不知道当前是登录还是注册模式
- 注册时没有确认密码输入框，输错密码直接创建了一个废号
- 非标准交互，用户预期是分开的 tab

**需求**：
- 登录和注册拆为独立 tab 页
- 登录 tab ：「用户名」+「密码」+ 「登录」按钮 + 「忘记密码？」链接 + 「没有账号？去注册」
- 注册 tab ：「用户名」+「昵称」+「密码」+「确认密码」+ 「注册」按钮 + 「已有账号？去登录」
- 注册时前端校验两次密码一致
- 注册成功后自动切换到登录 tab

**验收标准**：用户能在 3 秒内分辨当前是登录还是注册模式。

---

### 1.2 忘记密码入口

**现状**：完全没有。

**需求**：在登录表单下方增加「忘记密码？」链接。现阶段因缺 SMTP 服务无法发送重置邮件，改为：
- 点击后弹窗提示：「请联系管理员重置密码。管理员邮箱：admin@greengrassland.com」
- 后续接入邮件服务后替换为验证码重置流程

**验收标准**：用户在登录页能找到密码找回入口。

---

### 1.3 服务条款与隐私政策

**现状**：无。

**需求**：
- 注册表单底部增加复选框：「我已阅读并同意《服务条款》和《隐私政策》」
- 复选框未勾选时注册按钮置灰
- 点击链接在新标签页打开条款页面（先放占位文本，后续补充完整法律文本）
- 页面底部 footer 增加「服务条款」「隐私政策」链接

**验收标准**：注册时必须勾选同意条款才能提交。

---

## 二、P1 — 核心体验缺陷

### 2.1 智能推荐 v1

**现状**：「你可能感兴趣」区域实际展示的是最新的 4 条活动，等于没有推荐。

**需求**：
- 后端新增 `/api/post/recommend` 接口，参数 `userId`（可选，未登录返回热门）
- 推荐算法 v1（基于现有数据）：
  - 已登录：优先展示用户报名过的活动类型（如打过球就推更多球类）
  - 未登录：按报名人数降序（热门活动优先）
  - 排除已过期、已取消、已满员的活动
  - 返回 6 条，前端展示 4 条
- 前端增加「换一批」按钮，点击刷新推荐

**验收标准**：推荐结果与用户行为有关联，而非简单的时间倒序。

---

### 2.2 活动默认封面图

**现状**：不传图片的活动显示紫色渐变方块 + 📷 emoji，像占位 bug。

**需求**：
- 按活动类型生成不同的默认封面：
  - BALL_GAME → 篮球场插画背景
  - BOARD_GAME → 桌游插画背景
  - PET_SOCIAL → 宠物插画背景
  - GROUP_ACTIVITY → 聚会插画背景
  - STUDY_GROUP → 书本插画背景
- 使用 CSS 渐变 + 大号 emoji + 类型名称，有设计感
- 或使用 unsplash 随机图片作为默认背景

**验收标准**：无图活动的卡片看起来是有意设计，而非 bug。

---

### 2.3 交互式新用户引导

**现状**：一个弹窗 5 行文字，点关闭永远消失。

**需求**：
- 保留首次弹窗（已有，微调文案）
- 增加**步骤式引导**（3 步），在用户首次完成关键操作时触发：
  - 第 1 步：用户首次看到活动列表 → 高亮搜索框，提示"搜索你感兴趣的活动"
  - 第 2 步：用户首次打开活动详情 → 高亮报名按钮，提示"报名即可加入群聊"
  - 第 3 步：用户首次报名成功 → 高亮消息 tab，提示"去群聊和大家打个招呼吧"
- 每步引导有「跳过」和「下一步」
- 引导状态存 localStorage，完成或跳过后不再出现

**验收标准**：新用户完成 3 个关键操作后有明确的行为路径认知。

---

### 2.4 发布者信任信息

**现状**：活动卡片上只有发布者头像+昵称，没有任何信任信号。

**需求**：
- 在活动卡片发布者信息区增加：
  - 发布者历史活动数（如 "已发起 12 个活动"）
  - 发布者被点赞/收藏总数
- 数据从 UserDTO 已有字段获取（postCount, likeCount），仅需前端展示

**验收标准**：用户看到发布者信息后能判断其活跃度和可信度。

---

## 三、P2 — 用户体验细节

### 3.1 首页 Hero 区域

**现状**：首页直接是搜索框+活动列表。

**需求**：
- 未登录用户打开首页时，在搜索框上方展示 Hero 区域：
  - 大标题：「找到你的校园搭子」
  - 副标题：「打球、桌游、宠物、学习...和兴趣相同的人一起玩」
  - CTA 按钮：「立即加入」（跳转到注册/登录）
- 已登录用户不展示 Hero，直接看活动列表
- Hero 区域支持暗色模式

**验收标准**：未登录用户 3 秒内理解这个平台是做什么的。

---

### 3.2 搜索自动补全

**现状**：搜索全靠手动输入。

**需求**：
- 用户在搜索框输入时，显示下拉建议列表
- 建议来源：
  - 搜索历史（已有 localStorage）
  - 热门搜索词（从已有活动标题中提取高频词，前端计算）
- 点击建议项直接搜索
- 键盘上下键选择建议项，回车确认

**验收标准**：用户输入 2 个字符后能看到相关建议。

---

### 3.3 活动状态驱动的操作按钮

**现状**：活动结束后报名按钮还显示但点击报错。

**需求**：
- 活动状态为 FINISHED/CANCELLED 时：
  - 隐藏报名/取消报名按钮
  - 显示灰色「已结束」或「已取消」标签
- 活动状态为 FULL 时：
  - 报名按钮变为「已满员」（置灰不可点击）
- 只有 RECRUITING 状态才显示可点击的报名按钮

**验收标准**：用户看到的活动操作按钮与其当前状态一致，不会点了才报错。

---

### 3.4 活动分享海报

**现状**：只有复制链接。

**需求**：
- 活动详情页增加「生成海报」按钮
- 海报包含：
  - 活动标题、类型、时间、地点
  - 二维码（指向活动详情页）
  - GreenGrassland logo + 名称
- 前端用 Canvas 生成，可长按保存（移动端）

**验收标准**：用户能生成并保存活动海报图片。

---

## 四、技术债务治理

### 4.1 前端模块化

**现状**：123 个函数全在全局 window 对象上，app.js 2746 行单文件。

**需求**：
- 按功能拆分为独立 JS 文件：
  - `app.js` — 入口 + 全局状态
  - `auth.js` — 登录/注册/登出
  - `post.js` — 活动列表/详情/发布
  - `chat.js` — 私聊/群聊/WebSocket
  - `profile.js` — 用户主页/资料编辑
  - `search.js` — 搜索/筛选/排序
  - `ui.js` — 暗色模式/骨架屏/通知/导航
- 使用 ES Module（`type="module"`）加载
- 保持现有功能全部正常

**验收标准**：单文件不超过 500 行，函数不挂 window，通过 import/export 通信。

---

### 4.2 前端错误监控

**现状**：JS 报错用户看不到，开发者也看不到。

**需求**：
- 全局 `window.onerror` 捕获未处理异常
- 全局 `unhandledrejection` 捕获 Promise 异常
- 错误信息包含：错误消息、文件、行号、时间戳
- 开发环境 console.error 输出
- 生产环境通过 `navigator.sendBeacon` 发送到 `/api/error/report`
- 错误不影响用户正常使用（静默上报）

**验收标准**：所有前端异常都能被捕获并上报。

---

### 4.3 数据库表名修复

**现状**：`user` 是 SQL 保留关键字。

**需求**：
- 创建 Flyway 迁移脚本 V2，执行 `RENAME TABLE user TO users`
- 更新 `User.java` 的 `@Table(name = "users")`
- 更新所有引用 `user` 表的原生 SQL 和 JPQL
- 测试通过后部署

**验收标准**：任意 SQL 数据库（MySQL/PostgreSQL/H2）均可正常运行。

---

## 五、迭代计划

| 版本 | 内容 | 预计工时 |
|------|------|----------|
| v2.0 | P0 全部（登录分离/忘记密码/服务条款） | 2h |
| v2.1 | P1 全部（推荐/默认图/引导/信任信息） | 4h |
| v2.2 | P2 前 3 项（Hero/搜索建议/状态按钮） | 3h |
| v2.3 | P2 海报 + 技术债务 4.1 | 4h |
| v3.0 | 技术债务 4.2/4.3 + 全面测试 | 3h |

**总计约 16 小时，可在 2-3 天内完成。**
